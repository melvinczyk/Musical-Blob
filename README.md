# Musical-Blob
A deep learning project that converts song snippets into colored images, depending on the mood of the song.

# Getting Data

Go to the `data_scraping` dir and either download `data.zip` and extract `songs.csv` and `tags.csv` into `./data/csv`